/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "bigbangcasino.bet",
      },
    ],
  },
};

export default nextConfig;
