"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";
export default function Register(){const r=useRouter();const [form,setForm]=useState({displayName:'',email:'',password:''});const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const res=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});const b=await res.json();if(!res.ok){setError(b?.error?.message??'Não foi possível criar a conta.');setLoading(false);return}r.push('/?registered=1');r.refresh()}return <main className="min-h-[calc(100vh-72px)] grid place-items-center px-5 py-12"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-base-line bg-base-surface p-7 shadow-2xl"><p className="text-xs font-bold uppercase tracking-[.2em] text-gold">BETWIN STUDIOS</p><h1 className="mt-2 font-display text-3xl font-semibold">Criar conta</h1><p className="mt-2 text-sm text-ink-muted">Seu cadastro ficará salvo no banco da plataforma.</p><div className="mt-7 space-y-4"><input value={form.displayName} onChange={e=>setForm({...form,displayName:e.target.value})} placeholder="Nome" className="w-full rounded-xl border border-base-line bg-base px-4 py-3 text-sm"/><input value={form.email} onChange={e=>setForm({...form,email:e.target.value})} type="email" placeholder="E-mail" required className="w-full rounded-xl border border-base-line bg-base px-4 py-3 text-sm"/><input value={form.password} onChange={e=>setForm({...form,password:e.target.value})} type="password" placeholder="Senha (mínimo 8 caracteres)" minLength={8} required className="w-full rounded-xl border border-base-line bg-base px-4 py-3 text-sm"/>{error&&<p className="text-sm text-lose">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-gold px-4 py-3 font-bold text-base disabled:opacity-60">{loading?'Criando…':'Criar conta'}</button></div><p className="mt-6 text-center text-sm text-ink-muted">Já tem conta? <Link className="font-semibold text-gold" href="/login">Entrar</Link></p></form></main>}
