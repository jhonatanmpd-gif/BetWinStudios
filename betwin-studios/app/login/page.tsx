"use client";
import {FormEvent,useState} from "react";
import {useRouter} from "next/navigation";
import Link from "next/link";
export default function Login(){const r=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError("");const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const b=await res.json();if(!res.ok){setError(b?.error?.message??'Não foi possível entrar.');setLoading(false);return}r.push(b.user.role==='ADMIN'?'/admin':'/');r.refresh()}return <main className="min-h-[calc(100vh-72px)] grid place-items-center px-5 py-12"><form onSubmit={submit} className="w-full max-w-md rounded-3xl border border-base-line bg-base-surface p-7 shadow-2xl"><p className="text-xs font-bold uppercase tracking-[.2em] text-gold">BETWIN STUDIOS</p><h1 className="mt-2 font-display text-3xl font-semibold">Entrar</h1><p className="mt-2 text-sm text-ink-muted">Acesse sua conta para continuar.</p><div className="mt-7 space-y-4"><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="Seu e-mail" required className="w-full rounded-xl border border-base-line bg-base px-4 py-3 text-sm"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Sua senha" required className="w-full rounded-xl border border-base-line bg-base px-4 py-3 text-sm"/>{error&&<p className="text-sm text-lose">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-gold px-4 py-3 font-bold text-base disabled:opacity-60">{loading?'Entrando…':'Entrar'}</button></div><p className="mt-6 text-center text-sm text-ink-muted">Ainda não tem conta? <Link className="font-semibold text-gold" href="/register">Criar conta</Link></p></form></main>}
